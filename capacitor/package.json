{
  "name": "minesweeper-capacitor",
  "version": "1.0.0",
  "description": "Minesweeper mobile app powered by Capacitor",
  "scripts": {
    "serve": "npx http-server www -p 5173 -c-1",
    "sync": "npx cap sync",
    "open:ios": "npx cap open ios",
    "open:android": "npx cap open android",
    "add:ios": "npx cap add ios",
    "add:android": "npx cap add android"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0",
    "@capacitor/android": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "http-server": "^14.1.1"
  }
}
